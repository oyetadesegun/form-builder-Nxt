'use client';

import { FormElementInstance } from "./FormElements";

function FormSubmitComponent({
    formUrl,
    content,
}:{content:FormElementInstance[];
    formUrl: string
}) {
  return (
    <div>Form Submit Component</div>
  )
}

export default FormSubmitComponent